<div class="space-y-8">
  <div>
    <h1 class="text-3xl font-semibold tracking-tight text-gray-900">Campaigns</h1>
    <p class="mt-2 text-base text-gray-600">Create, dispatch, and track campaign progress.</p>
  </div>

  <div class="card">
    <div class="card-header">
      <h2 class="text-base font-semibold text-gray-900">All campaigns</h2>
    </div>

    <% if @campaigns.any? %>
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th scope="col" class="px-6 py-3 text-left text-xs font-semibold uppercase tracking-wide text-gray-600">Title</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-semibold uppercase tracking-wide text-gray-600">Status</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-semibold uppercase tracking-wide text-gray-600">Progress</th>
              <th scope="col" class="px-6 py-3 text-right text-xs font-semibold uppercase tracking-wide text-gray-600">Actions</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-200 bg-white">
            <% @campaigns.each do |campaign| %>
              <% total = campaign.recipients.size %>
              <% sent = campaign.recipients.count(&:sent?) %>
              <tr class="hover:bg-gray-50">
                <td class="whitespace-nowrap px-6 py-4 text-sm font-medium text-gray-900">
                  <%= link_to campaign.title, campaign_path(campaign), class: "text-indigo-600 hover:text-indigo-500" %>
                </td>
                <td class="whitespace-nowrap px-6 py-4 text-sm"><%= campaign_status_badge(campaign.status) %></td>
                <td class="whitespace-nowrap px-6 py-4 text-sm text-gray-700">
                  <span class="font-medium"><%= sent %></span>/<span class="font-medium"><%= total %></span> sent
                </td>
                <td class="whitespace-nowrap px-6 py-4 text-right text-sm">
                  <div class="flex justify-end gap-3">
                    <%= link_to "View", campaign_path(campaign), class: "font-medium text-gray-700 hover:text-gray-900" %>
                    <% if campaign.pending? %>
                      <%= link_to "Edit", edit_campaign_path(campaign), class: "font-medium text-gray-700 hover:text-gray-900" %>
                    <% else %>
                      <span class="font-medium text-gray-400">Edit</span>
                    <% end %>
                    <%= button_to "Delete",
                          campaign_path(campaign),
                          method: :delete,
                          form: { data: { turbo_confirm: "Delete this campaign?" } },
                          class: "font-medium text-red-600 hover:text-red-500" %>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% else %>
      <div class="card-body">
        <div class="flex flex-col items-start justify-between gap-6 sm:flex-row sm:items-center">
          <div>
            <p class="text-base font-semibold text-gray-900">No campaigns yet</p>
            <p class="mt-1 text-sm text-gray-600">Create a campaign to start dispatching recipients.</p>
          </div>

          <%= link_to "Create your first campaign",
                new_campaign_path,
                class: "btn btn-md btn-primary" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
