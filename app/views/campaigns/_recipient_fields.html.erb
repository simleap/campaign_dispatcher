<div class="rounded-md border border-gray-200 bg-white p-4" data-nested-form-target="row">
  <%= f.hidden_field :_destroy %>

  <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
    <div>
      <%= f.label :name, class: "block text-sm font-medium text-gray-700" %>
      <%= f.text_field :name,
            required: true,
            class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500" %>
      <% if f.object.errors[:name].any? %>
        <p class="mt-1 text-sm text-red-600"><%= f.object.errors[:name].first %></p>
      <% end %>
    </div>

    <div>
      <%= f.label :contact, class: "block text-sm font-medium text-gray-700" %>
      <%= f.text_field :contact,
            required: true,
            class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500" %>
      <% if f.object.errors[:contact].any? %>
        <p class="mt-1 text-sm text-red-600"><%= f.object.errors[:contact].first %></p>
      <% end %>
    </div>
  </div>

  <div class="mt-3 flex items-center justify-end">
    <button
      type="button"
      data-action="nested-form#remove"
      class="text-sm font-medium text-gray-600 hover:text-gray-900 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
      Remove
    </button>
  </div>
</div>

